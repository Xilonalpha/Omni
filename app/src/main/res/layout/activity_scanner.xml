<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/black"
    tools:context=".ui.ScannerActivity">

    <!-- Camera Preview (INTACT) -->
    <androidx.camera.view.PreviewView
        android:id="@+id/view_finder"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent" />

    <!-- HUD Overlay View (INTACT) -->
    <com.chemscanner.omniscient.ui.scanner.ScannerHUDView
        android:id="@+id/scanner_hud"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <!-- Data Windows (ENHANCED) -->
    <LinearLayout
        android:id="@+id/data_panel"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_margin="24dp"
        android:background="#99000000"
        android:padding="8dp"
        android:orientation="vertical"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent">
        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="PRECISION: ULTRA"
            android:textColor="#00FFFF"
            android:textSize="10sp"
            android:textStyle="bold"/>
        <TextView
            android:id="@+id/hud_bit_flux"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="FLUX: 452 KB/S"
            android:textColor="#FFFFFF"
            android:textSize="9sp"/>

        <TextView
            android:id="@+id/emf_interference_txt"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:text="EMF: CALIBRATING..."
            android:textColor="#FFD700"
            android:textSize="8sp"
            android:textStyle="bold"/>
    </LinearLayout>

    <!-- REAL-TIME OBJECT TAG (Position Tuned) -->
    <TextView
        android:id="@+id/object_tag_txt"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="120dp"
        android:background="#AA000000"
        android:padding="8dp"
        android:text="ANALYZING SPECTRA..."
        android:textColor="#00FF00"
        android:textSize="12sp"
        android:textStyle="bold"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <!-- LIVE SPECTROMETER OVERLAY (INTACT) -->
    <LinearLayout
        android:id="@+id/spectrometer_visualizer"
        android:layout_width="match_parent"
        android:layout_height="40dp"
        android:layout_marginBottom="120dp"
        android:orientation="horizontal"
        android:gravity="bottom"
        android:paddingHorizontal="40dp"
        app:layout_constraintBottom_toBottomOf="parent">
        <View android:layout_width="4dp" android:layout_height="10dp" android:layout_margin="1dp" android:background="#00FFFF" android:alpha="0.5"/>
        <View android:layout_width="4dp" android:layout_height="25dp" android:layout_margin="1dp" android:background="#00FFFF" android:alpha="0.5"/>
        <View android:layout_width="4dp" android:layout_height="15dp" android:layout_margin="1dp" android:background="#00FFFF" android:alpha="0.5"/>
        <View android:layout_width="4dp" android:layout_height="30dp" android:layout_margin="1dp" android:background="#00FFFF" android:alpha="0.5"/>
        <View android:layout_width="4dp" android:layout_height="20dp" android:layout_margin="1dp" android:background="#00FFFF" android:alpha="0.5"/>
    </LinearLayout>

    <!-- Top Controls (INTACT) -->
    <LinearLayout
        android:id="@+id/top_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:orientation="horizontal"
        android:paddingHorizontal="16dp"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent">
        <ImageButton
            android:id="@+id/close_button"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="center"
            android:src="@android:drawable/ic_menu_close_clear_cancel"
            app:tint="@android:color/white" />
    </LinearLayout>

    <!-- Bottom Controls (INTACT) -->
    <LinearLayout
        android:id="@+id/bottom_controls"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="32dp"
        android:gravity="center"
        android:orientation="horizontal"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toStartOf="parent">
        <ImageButton
            android:id="@+id/gallery_button"
            android:layout_width="64dp"
            android:layout_height="64dp"
            android:layout_marginEnd="32dp"
            android:background="?attr/selectableItemBackgroundBorderless"
            android:scaleType="center"
            android:src="@android:drawable/ic_menu_gallery"
            app:tint="@android:color/white" />
        <Button
            android:id="@+id/capture_button"
            android:layout_width="80dp"
            android:layout_height="80dp"
            android:background="@android:drawable/btn_default"
            android:text="SCAN"
            android:textColor="#000000"
            android:textStyle="bold"
            android:textSize="14sp" />
    </LinearLayout>

    <ProgressBar
        android:id="@+id/progress_bar"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:indeterminateTint="#00FFFF"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

    <TextView
        android:id="@+id/error_message"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:visibility="gone"
        android:background="#E6000000"
        android:textColor="#FF0000"
        android:padding="16dp"
        android:layout_marginBottom="16dp"
        android:textStyle="bold"
        app:layout_constraintBottom_toTopOf="@+id/bottom_controls"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintEnd_toEndOf="parent" />

</androidx.constraintlayout.widget.ConstraintLayout>
